<form method="post" action="{{ path('kendaraan_delete', {'id': kendaraan.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ kendaraan.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
